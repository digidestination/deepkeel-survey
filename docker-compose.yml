services:
  deepkeel:
    build: .
    container_name: deepkeel-app
    env_file:
      - .env
    ports:
      - "9890:9890"
    volumes:
      - ./data:/app/data
      - ./uploads:/app/uploads
    restart: unless-stopped
